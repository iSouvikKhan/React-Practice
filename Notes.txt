Navigate replace to="/navigatedcomponent" :
        --> it should call the "navigatedcomponent" as a separate component
        --> not as associated with it


Link :
        --> redirects smoothly without refreshing the page


Outlet :
        --> is required for getting the output below the button